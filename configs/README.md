# Configs

This directory owns experiment definitions.

- `experiment/`: runnable experiment matrices and task-specific config assets.
- `remote/`: remote workstation launch specifications.

Python implementation belongs under `src/blockcipher_nd/`; generated run
artifacts belong under `outputs/` or `runs/`, not in this directory.
